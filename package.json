{
  "name": "ohmyscribe",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "ingestion"
  ],
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "prettier": "^3.9.4"
  }
}
